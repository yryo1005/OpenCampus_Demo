# AI着色デモ（Image Colorization）使い方

高校生向けオープンキャンパス用の，**顔写真をモノクロにしてから AI で色を復元する**デモです．  
Google Colab（GPU: T4 推奨）上で DDColor を動かし，Gradio の画面からカメラ撮影や画像を渡して着色します．

| 項目 | 内容 |
| :--- | :--- |
| ノートブック | `OC_Colorize.ipynb` |
| 主な技術 | DDColor（ICCV 2023），Lab 色空間，Gradio |
| API キー | **不要**（すべて Colab 内で推論） |
| Hugging Face 認証 | **不要**（公開モデルを自動ダウンロード） |
| 想定環境 | Google Colab，T4（VRAM 約 14GB） |
| 出力 | 元画像，モノクロ画像，着色画像，三点比較 |

---

## デモでできること

1. **カメラ**で顔を撮影して着色する  
2. 画像ファイルを**アップロード**して着色する  
3. 用意した**サンプル顔写真**（日本人・アジア系を含む）をクリックしてすぐ試せる  
4. **元画像 / モノクロ / AI着色**を並べて，本物の色と AI の色を比べる  

処理の流れは次のとおりです．

1. カラーの顔写真を入力する  
2. プログラムが**意図的にモノクロ化**する  
3. DDColor が色（あかさ・あおさなど）を推定して着色する  

---

## Colab での実行手順

### 1. ランタイムを GPU にする

1. ノートブックを Colab で開く  
2. メニュー **ランタイム → ランタイムのタイプを変更**  
3. ハードウェア アクセラレータを **T4 GPU** にする  

### 2. セルを上から実行する

途中で止まっても再開しやすいよう，処理は次の段に分かれています．

| 順番 | セル | 内容 |
| :---: | :--- | :--- |
| 0 | 設定 | Webカメラの左右反転・モデル名・入力解像度 |
| 1 | ライブラリのインストール | DDColor リポジトリのクローン，`tqdm` / `huggingface_hub` |
| 2 | ライブラリの読み込み・初期化 | サンプル画像 DL，DDColor モデル読込 |
| 3 | Gradio の実行 | Web UI を起動する |

**Webカメラが左右反転しているとき**  
ノートブック上部の設定セルで `MIRROR_WEBCAM = True` / `False` を切り替え，Gradio 起動セルを再実行してください．

**注意（インストール直後）**  
インストールのあとでインポートエラーが出る場合は，**ランタイム → セッションを再起動**し，設定セルとセル2からやり直してください．

### 3. UI の使い方

Gradio が起動すると，Colab 内に UI が表示されます（`share=True` のため公開用 URL も出ます）．

1. **サンプル画像**をクリックする，またはカメラで撮影／ファイルをアップロードする  
2. **AI着色**ボタンを押す（画像を選んだだけで自動実行される場合もあります）  
3. 右側・下側に  
   - **元画像 / モノクロ / AI着色**の三点比較  
   - それぞれの単独表示  
   - **説明**（モデル名・解像度など）  
   が表示されます  

着色はおおよそ数秒以内です（初回のモデル読込後）．

---

## モデルの違い

設定セルの `MODEL_NAME` で切り替えられます（変更後はセル2以降を再実行）．

| モデル名 | イメージ |
| :--- | :--- |
| `ddcolor_modelscope`（推奨） | 画質重視．一般的な写真向け |
| `ddcolor_artistic` | アーティスティック寄り |
| `ddcolor_paper_tiny` | 軽量・高速（デモの待ち時間を短くしたいとき） |
| `ddcolor_paper` | 論文の再現用 |

---

## うまく着色させるコツ

- 顔が**正面**を向き，明るく，画面内で大きめに写るようにする  
- 極端な逆光やぼやけた写真は色の推定が難しくなります  
- 元がカラーの写真なら，AI の色と本物の色を比べてみてください（一致しなくて当たり前です）  
- もともとモノクロの写真を入れると，「元画像」と「モノクロ」がほぼ同じになります  

---

## 技術のポイント（説明用）

- **モノクロ化**: 明るさ（輝度）だけを残し，色の情報を消す  
- **Lab 色空間**: $L$ が明るさ，$a$・$b$ が色．DDColor はモノクロから $a$・$b$ を推定する  
- **DDColor**: デュアルデコーダで写真らしい色を復元する生成モデル（ICCV 2023）  
- 推論は Colab の GPU 上で完結し，外部の画像生成 API は使いません  

---

## サンプル画像について

初期化セルで次の公開画像をダウンロードします（日本人・アジア系を含む）．

| ファイル | 内容 |
| :--- | :--- |
| `happy_japanese_woman.jpg` | 笑顔（Wikimedia・日本人） |
| `happy_japanese_smile.jpg` | スマイル（Wikimedia・日本人） |
| `neutral_japanese.jpg` | ポートレート（Wikimedia・日本人） |
| `asian_portrait.jpg` ほか | Pexels の公開ポートレート |

カメラが使えない環境でも，サンプルだけでデモできます．

---

## Hugging Face / 認証について

このデモの重みは次の公開リポジトリから取得します．

- https://huggingface.co/piddnad/ddcolor_modelscope （既定）
- ほか: `ddcolor_artistic` / `ddcolor_paper_tiny` / `ddcolor_paper`

コードは https://github.com/piddnad/DDColor をクローンして利用します．

**現時点ではユーザー認証やライセンス同意の操作は不要**です．  
Hugging Face アカウントも不要です．

---

## トラブルシュート

| 症状 | 対処 |
| :--- | :--- |
| 色がおかしい／くすむ | 別モデル（`artistic` や `tiny`）を試す．明るい正面顔にする |
| カメラが使えない | ブラウザのカメラ許可を確認．難しければアップロード／サンプルを使う |
| `CUDA out of memory` | ランタイム再起動．`INPUT_SIZE` を 256，`MODEL_NAME` を `ddcolor_paper_tiny` にする |
| モデル DL に失敗 | ネット接続を確認．セル2を再実行 |
| `DDColor が見つかりません` | セル1（インストール）を再実行 |
| インストール後に import できない | セッション再起動 → セル2から再実行 |

---

## ライセンスメモ

利用する画像・モデルの条件は，各 Wikimedia / Pexels / DDColor / Hugging Face リポジトリの表記に従ってください．  
オープンキャンパス等の教育・デモ用途を想定しています．
