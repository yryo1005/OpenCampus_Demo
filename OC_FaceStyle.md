# 顔スタイル変換デモ（Face Style Transfer）使い方

高校生向けオープンキャンパス用の，**顔写真をアニメキャラ風・絵画風に変換する**デモです．  
Google Colab（GPU: T4 推奨）上で AnimeGANv2 を動かし，Gradio の画面からカメラ撮影や画像を渡してスタイル変換します．

| 項目 | 内容 |
| :--- | :--- |
| ノートブック | `OC_FaceStyle.ipynb` |
| 主な技術 | AnimeGANv2（`torch.hub`），OpenCV 顔検出，Gradio |
| API キー | **不要**（すべて Colab 内で推論） |
| Hugging Face 認証 | **不要**（重みは GitHub から取得） |
| 想定環境 | Google Colab，T4（VRAM 約 14GB） |
| 選べるスタイル | アニメキャラ風／アニメ似顔絵風／やさしいイラスト風／映画イラスト・絵画風 |

---

## デモでできること

1. **カメラ**で顔を撮影してスタイル変換する  
2. 画像ファイルを**アップロード**して変換する  
3. 用意した**サンプル顔写真**（日本人・アジア系を含む）をクリックしてすぐ試せる  
4. スタイルを切り替えて，**変換前／変換後**の比較を見る  

---

## Colab での実行手順

### 1. ランタイムを GPU にする

1. ノートブックを Colab で開く  
2. メニュー **ランタイム → ランタイムのタイプを変更**  
3. ハードウェア アクセラレータを **T4 GPU** にする  

### 2. セルを上から実行する

途中で止まっても再開しやすいよう，処理は次の段に分かれています．

| 順番 | セル | 内容 |
| :---: | :--- | :--- |
| 0 | 設定 | Webカメラの左右反転・変換解像度・既定スタイル |
| 1 | ライブラリのインストール | `tqdm` を更新（本体は Colab 標準パッケージ） |
| 2 | ライブラリの読み込み・初期化 | サンプル画像 DL，顔検出・4 スタイルのモデル読込 |
| 3 | Gradio の実行 | Web UI を起動する |

**Webカメラが左右反転しているとき**  
ノートブック上部の設定セルで `MIRROR_WEBCAM = True` / `False` を切り替え，Gradio 起動セルを再実行してください．

**注意（インストール直後）**  
インストールのあとでインポートエラーが出る場合は，**ランタイム → セッションを再起動**し，設定セルとセル2からやり直してください．

### 3. UI の使い方

Gradio が起動すると，Colab 内に UI が表示されます（`share=True` のため公開用 URL も出ます）．

1. **サンプル画像**をクリックする，またはカメラで撮影／ファイルをアップロードする  
2. **スタイル**を選ぶ（アニメキャラ風がおすすめ）  
3. **スタイル変換**ボタンを押す（画像を選んだだけで自動実行される場合もあります）  
4. 右側に  
   - **変換結果**  
   - **変換前 / 変換後**の比較  
   - **説明**（スタイル名・切り出し方法など）  
   が表示されます  

変換はおおよそ数秒以内です（初回のモデル読込後）．

---

## スタイルの違い

| 表示名 | モデルキー | イメージ |
| :--- | :--- | :--- |
| アニメキャラ風（推奨） | `face_paint_512_v2` | 顔写真をアニメのキャラクター風に |
| アニメ似顔絵風 | `face_paint_512_v1` | やや綺麗めのアニメ似顔絵 |
| やさしいイラスト風 | `celeba_distill` | 柔らかい線のイラスト寄り |
| 映画イラスト・絵画風 | `paprika` | 映画『パプリカ』風の絵画寄りのタッチ |

---

## うまく変換させるコツ

- 顔が**正面**を向き，明るく，画面内で大きめに写るようにする  
- マスク・強い逆光・横顔は検出や変換が難しくなります  
- 顔が検出できない場合は画像の中央を切り出して変換します（結果が弱くなることがあります）  
- スタイルを変えると印象が大きく変わるので，同じ写真でいくつか試してみてください  

---

## 技術のポイント（説明用）

- **顔検出**: 画像のどこに顔があるかを見つける（このデモでは OpenCV の Haar Cascade）  
- **スタイル変換（画像から画像）**: 写真の構図を保ちつつ，線や色の「画風」を別の見た目に変える  
- **AnimeGANv2**: 顔写真向けに学習された生成モデル．拡散モデルより軽く，T4 でも高速  
- 推論は Colab の GPU 上で完結し，外部の画像生成 API は使いません  

---

## サンプル画像について

初期化セルで次の公開画像をダウンロードします（日本人・アジア系を含む）．

| ファイル | 内容 |
| :--- | :--- |
| `happy_japanese_woman.jpg` | 笑顔（Wikimedia・日本人） |
| `happy_japanese_smile.jpg` | スマイル（Wikimedia・日本人） |
| `neutral_japanese.jpg` | ポートレート（Wikimedia・日本人） |
| `asian_portrait.jpg` ほか | Pexels の公開ポートレート |

カメラが使えない環境でも，サンプルだけでデモできます．

---

## Hugging Face / 認証について

このデモの重みは次の GitHub リポジトリから `torch.hub` 経由で取得します．

- https://github.com/bryandlee/animegan2-pytorch

**現時点ではユーザー認証やライセンス同意の操作は不要**です．  
Hugging Face アカウントも不要です．

---

## トラブルシュート

| 症状 | 対処 |
| :--- | :--- |
| 顔を検出できない | 明るさ・距離・向きを見直す．正面で大きく写す |
| カメラが使えない | ブラウザのカメラ許可を確認．難しければアップロード／サンプルを使う |
| 変換が弱い／崩れる | 別スタイルを試す．顔が大きく写る写真にする |
| `CUDA out of memory` | ランタイム再起動．`PAINT_SIZE` を 512 のままにする |
| `torch.hub` で失敗 | ネット接続を確認．セル2を再実行 |
| インストール後に import できない | セッション再起動 → セル2から再実行 |

---

## ライセンスメモ

利用する画像・モデルの条件は，各 Wikimedia / Pexels / AnimeGANv2 リポジトリの表記に従ってください．  
オープンキャンパス等の教育・デモ用途を想定しています．
