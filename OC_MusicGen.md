# 音楽生成デモ（MusicGen）使い方

高校生向けオープンキャンパス用の，**テキストから音楽を生成する**デモです．  
Google Colab（GPU: T4 推奨）上で Meta の MusicGen を動かし，Gradio の画面から日本語／英語のプロンプトを渡して短い音楽クリップを作ります．

| 項目 | 内容 |
| :--- | :--- |
| ノートブック | `OC_MusicGen.ipynb` |
| 主な技術 | MusicGen（Transformers），Gemini（日本語→英語翻訳），Gradio |
| API キー | **Gemini が必要**（`tokens.json` の `gemini` キー） |
| Hugging Face 認証 | **現時点では不要**（モデルは公開） |
| 想定環境 | Google Colab，T4（VRAM 約 14GB） |

---

## デモでできること

1. **日本語**または**英語**で，ジャンル・雰囲気・楽器などを文章で指定する  
2. 日本語の場合は Gemini が **英語に翻訳**する（翻訳結果も画面に表示）  
3. MusicGen が **数秒〜十数秒の音楽**を生成する  
4. 用意した**サンプルプロンプト**をクリックしてすぐ試せる  

---

## 事前準備：`tokens.json`

ノートブックと同じ場所に，次の形式の `tokens.json` を置いてください（リポジトリには含まれません）．

```json
{
  "gemini": "ここに Gemini API キー"
}
```

Colab で GitHub バッジから開いた場合は，左のファイル欄へ `tokens.json` をアップロードしてください．

---

## Hugging Face について

このデモで使うモデルは次の公開リポジトリです．

- https://huggingface.co/facebook/musicgen-small

**現時点ではユーザー認証やライセンス同意のゲートは不要**です．  
将来モデルが gated になった場合は，上記ページでアクセス申請し，必要なら Hugging Face トークンでログインしてください．

ライセンスは **CC-BY-NC 4.0**（非営利）です．オープンキャンパス等の教育・デモ用途を想定しています．

---

## Colab での実行手順

### 1. ランタイムを GPU にする

1. ノートブックを Colab で開く  
2. メニュー **ランタイム → ランタイムのタイプを変更**  
3. ハードウェア アクセラレータを **T4 GPU** にする  

### 2. セルを上から実行する

途中で止まっても再開しやすいよう，処理は次の段に分かれています．

| 順番 | セル | 内容 |
| :---: | :--- | :--- |
| 0 | 設定 | モデル ID・生成秒数・Gemini モデル名など |
| 1 | ライブラリのインストール | `transformers` / `google-genai` を更新 |
| 2 | ライブラリの読み込み・初期化 | `tokens.json` 読み込み，モデルダウンロード |
| 3 | Gradio の実行 | Web UI を起動する |

**注意（インストール直後）**  
インストールのあとでインポートエラーが出る場合は，**ランタイム → セッションを再起動**し，設定セルとセル2からやり直してください．

### 3. UI の使い方

Gradio が起動すると，Colab 内に UI が表示されます（`share=True` のため公開用 URL も出ます）．

1. **サンプルプロンプト**をクリックする，または自分で文章を書く  
2. （任意）生成秒数をスライダーで調整する（長いほど時間がかかる）  
3. （任意）シードを指定する（同じ雰囲気を再現したいとき．`-1` でランダム）  
4. **音楽を生成**ボタンを押す  
5. 右側で再生し，実際に使った英語プロンプトを確認する  

8 秒程度の生成には，T4 でおおよそ数十秒かかることがあります（モデルサイズや秒数による）．

---

## うまく書くコツ

- 「ジャンル・雰囲気・楽器」を短く書く（例: `静かなジャズピアノ，雨の日のカフェ`）  
- 英語で直接書いてもよい（翻訳はスキップされる）  
- 歌声付きの歌や，特定の既存楽曲の再現は苦手なことが多い  

---

## 技術のポイント（説明用）

- **テキスト音楽生成**: 文章の意味を手がかりに，音のトークン列を順に予測して波形を復元する  
- **MusicGen**: EnCodec で圧縮した音トークンを，Transformer がテキスト条件付きで生成する  
- **プロンプト翻訳**: モデルは英語キャプション中心に学習されているため，日本語は英語にしてから渡す  

---

## トラブルシュート

| 症状 | 対処 |
| :--- | :--- |
| `tokens.json` が見つからない | ファイルをアップロードし，カレントディレクトリを確認 |
| `CUDA out of memory` | ランタイム再起動．`DURATION_SEC` を短くする．`musicgen-small` のままにする |
| 生成が極端に遅い | ランタイムが GPU か確認．秒数を 5〜8 前後に |
| ノイズっぽい／期待と違う | プロンプトやシードを変えて再生成 |
| インストール後に import できない | セッション再起動 → セル2から再実行 |

---

## ライセンスメモ

MusicGen（facebook/musicgen-small）は **CC-BY-NC 4.0** です．  
営利利用は避け，教育・デモ用途で利用してください．有害・差別的な生成は避けてください．
