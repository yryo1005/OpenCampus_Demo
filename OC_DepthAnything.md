# 深度推定デモ（Depth Anything）使い方

高校生向けオープンキャンパス用の，**1枚の写真から手前と奥を色で可視化する**デモです．  
Google Colab（GPU: T4 推奨）上で Depth Anything V2 を動かし，Gradio の画面からカメラ撮影や画像を渡して深度推定します．

| 項目 | 内容 |
| :--- | :--- |
| ノートブック | `OC_DepthAnything.ipynb` |
| 主な技術 | Depth Anything V2（`small`），Transformers，Gradio，OpenCV |
| API キー | **不要**（すべて Colab 内で推論） |
| Hugging Face 認証 | **不要**（公開モデルを自動ダウンロード） |
| 想定環境 | Google Colab，T4（VRAM 約 14GB） |
| 出力 | 深度カラーマップ，原画像との横並び比較 |

---

## デモでできること

1. **カメラ**で撮影した写真の「手前／奥」を色分けする  
2. 画像ファイルを**アップロード**して深度を推定する  
3. 用意した**サンプル画像**（街並み・室内・風景・人物など）をクリックしてすぐ試せる  
4. カラーマップ（INFERNO / TURBO など）を切り替えて見え方を変える  

---

## Colab での実行手順

### 1. ランタイムを GPU にする

1. ノートブックを Colab で開く  
2. メニュー **ランタイム → ランタイムのタイプを変更**  
3. ハードウェア アクセラレータを **T4 GPU** にする  

### 2. セルを上から実行する

途中で止まっても再開しやすいよう，処理は次の段に分かれています．

| 順番 | セル | 内容 |
| :---: | :--- | :--- |
| 0 | 設定 | Webカメラの左右反転・モデルサイズ・カラーマップ |
| 1 | ライブラリのインストール | `transformers` の更新 |
| 2 | ライブラリの読み込み・初期化 | サンプル画像 DL，Depth Anything 読み込み |
| 3 | Gradio の実行 | Web UI を起動する |

**Webカメラが左右反転しているとき**  
ノートブック上部の設定セル，または UI の「左右反転（ミラー）」で切り替えてください．

**注意（インストール直後）**  
インストールのあとでインポートエラーが出る場合は，**ランタイム → セッションを再起動**し，設定セルとセル2からやり直してください．

**初回の待ち時間**  
Depth Anything V2 Small の重みを Hugging Face から取得するため，初回は数分かかることがあります．

### 3. UI の使い方

Gradio が起動すると，Colab 内に UI が表示されます（`share=True` のため公開用 URL も出ます）．

1. **サンプル画像**をクリックする，またはカメラで撮影／ファイルをアップロードする  
2. 必要なら色の付け方・左右反転を選ぶ  
3. **深度推定を実行**を押す  
4. 右側に深度マップと，原画像との横並びが表示される  

色の目安（既定の解釈）: **明るい色ほど手前，暗い色ほど奥**（相対深度）．

---

## うまく見せるコツ

- 手前に物体，奥に背景がある写真だと差がはっきりします  
- 街並みや室内，遠近のある風景が向いています  
- すべてが同じ距離の平面的な写真は，色の差が小さくなります  
- ブレや極端な暗所は推定が不安定になりやすいです  

---

## 技術のポイント（説明用）

- **深度推定（Depth Estimation）**: 1枚の写真から各ピクセルの遠近を推定する処理  
- **相対深度**: 「何メートル先か」ではなく，「どちらが手前か」の並び  
- **Depth Anything V2**: 大量の画像で学習された単眼深度推定モデル  
- モデル: [depth-anything/Depth-Anything-V2-Small-hf](https://huggingface.co/depth-anything/Depth-Anything-V2-Small-hf)  
- 推論は Colab の GPU 上で完結し，外部の深度 API は使いません  

---

## サンプル画像について

初期化セルで次の公開画像をダウンロードします（撮影しなくてもデモできます）．

| ファイル | 内容 |
| :--- | :--- |
| `street.jpg` | 街並み（Pexels） |
| `room.jpg` | 室内（Pexels） |
| `mountain.jpg` | 山・風景（Pexels） |
| `bicycle.jpg` | 自転車（Pexels） |
| `person.jpg` | 人物（Pexels） |
| `bridge.jpg` | 橋・遠景（Pexels） |

---

## Hugging Face について

モデルは Hugging Face 上の公開リポジトリから取得します．

- https://huggingface.co/depth-anything/Depth-Anything-V2-Small-hf  

**ユーザー認証やライセンス同意の操作は不要**です（ゲート付きモデルではありません）．

---

## トラブルシュート

| 症状 | 対処 |
| :--- | :--- |
| 推定が遅い | 設定の `MODEL_SIZE` を `small` にする．`MAX_IMAGE_SIDE` を下げる |
| 色の差が分かりにくい | UI のカラーマップを `TURBO` などに変更する |
| カメラが左右逆 | 「左右反転（ミラー）」を切り替える |
| カメラが使えない | ブラウザのカメラ許可を確認．難しければアップロード／サンプルを使う |
| `CUDA out of memory` | ランタイム再起動．`small` のままにする．`MAX_IMAGE_SIDE` を 768 などに下げる |
| インストール後に import できない | セッション再起動 → セル2から再実行 |

---

## ライセンスメモ

利用する画像・モデルの条件は，各 Pexels / Depth Anything の表記に従ってください．  
オープンキャンパス等の教育・デモ用途を想定しています．
