# 手書き文字認識デモ（YomiToku）使い方

高校生向けオープンキャンパス用の，**手書き日本語 OCR** デモです．  
Google Colab（GPU: T4 推奨）上で [YomiToku](https://github.com/kotaro-kinoshita/yomitoku) を動かし，Gradio の画面から画像を渡して文字起こしします．

| 項目 | 内容 |
| :--- | :--- |
| ノートブック | `OC_HandwritingOCR.ipynb` |
| 主な技術 | YomiToku（Document AI / 手書き文字認識），Gradio |
| API キー | **不要**（すべて Colab 内で推論） |
| 想定環境 | Google Colab，T4（VRAM 約 14GB） |

---

## デモでできること

1. 用意した**サンプル手書きメモ**を選んで文字起こしする  
2. **カメラ**でノートや付箋を撮影して文字起こしする  
3. 画像ファイルを**アップロード**して文字起こしする  
4. 検出した文字位置が枠で示された**可視化画像**を見る  

---

## Colab での実行手順

### 1. ランタイムを GPU にする

1. ノートブックを Colab で開く  
2. メニュー **ランタイム → ランタイムのタイプを変更**  
3. ハードウェア アクセラレータを **T4 GPU** にする  

### 2. セルを上から実行する

途中で止まっても再開しやすいよう，処理は次の段に分かれています．

| 順番 | セル | 内容 |
| :---: | :--- | :--- |
| 0 | 設定（Webカメラ・可視化） | `MIRROR_WEBCAM` / `VIS_FONT_SIZE` を指定 |
| 1 | ライブラリのインストール | `yomitoku` を `pip` で入れる |
| 2 | ライブラリの読み込み・初期化 | フォント／サンプル作成，モデル読み込み |
| 3 | Gradio の実行 | Web UI を起動する |

**Webカメラが左右反転しているとき**  
ノートブック上部の設定セルで `MIRROR_WEBCAM = True` / `False` を切り替え，Gradio 起動セルを再実行してください．`True` がミラー表示，`False` が反転なしです．

**可視化の文字が小さい／大きいとき**  
同じ設定セルの `VIS_FONT_SIZE`（既定 48）を変更し，**初期化セル**と Gradio 起動セルを再実行してください．

**注意（インストール直後）**  
インストールのあとでインポートエラーが出る場合は，**ランタイム → セッションを再起動**し，設定セルとセル2からやり直してください．

### 3. UI の使い方

Gradio が起動すると，Colab 内に UI が表示されます（`share=True` のため公開用 URL も出ます）．

1. **サンプル手書きメモ**をクリックする，または画像をアップロード／カメラ撮影する  
2. **文字起こし**ボタンを押す（画像を選んだだけで自動実行される場合もあります）  
3. 右側に  
   - **読み取り結果**（テキスト）  
   - **検出結果の可視化**（どこを文字と判定したか）  
   が表示されます  

---

## うまく読むコツ

- 紙全体が明るく，ピンボケしないように写す  
- 入力は前処理で**短辺 720px**にリサイズされます（カメラ解像度が異なっても揃います）  
- 極端なくずし字や細いペンは精度が落ちることがあります  

---

## 技術のポイント（説明用）

- **OCR（Optical Character Recognition）**: 画像の中の文字をコンピュータが読み取る技術  
- **YomiToku**: 日本語文書向けに学習された検知・認識モデルを組み合わせた Document AI  
- 手書きにも対応しており，印刷文字だけでなくノートの文字も扱えます  
- 推論は Colab の GPU 上で完結し，外部の文字認識 API は使いません  

---

## トラブルシュート

| 症状 | 対処 |
| :--- | :--- |
| `CUDA out of memory` | ランタイム再起動後，他の重いノートを閉じる |
| インストール後に import できない | セッション再起動 → セル2から再実行 |
| カメラが使えない | ブラウザのカメラ許可を確認．難しければアップロードを使う |
| 文字が空／誤読が多い | 明るさ・解像度・ pen の濃さを見直す |

---

## ライセンスメモ

YomiToku 本体およびモデルは **CC BY-NC-SA 4.0**（非商用）です．  
オープンキャンパス等の教育・デモ用途での利用を想定しています．
