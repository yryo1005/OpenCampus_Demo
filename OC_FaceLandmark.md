# 顔ランドマーク検出デモ（Face Landmark Detection）使い方

高校生向けオープンキャンパス用の，**顔写真から特徴点（ランドマーク）を検出する**デモです．  
Google Colab 上で MediaPipe Face Landmarker を動かし，Gradio の画面からカメラ撮影や画像を渡して，目・鼻・口・輪郭などの点とメッシュを可視化します．

| 項目 | 内容 |
| :--- | :--- |
| ノートブック | `OC_FaceLandmark.ipynb` |
| 主な技術 | MediaPipe Face Landmarker，OpenCV，Gradio |
| API キー | **不要**（すべて Colab 内で推論） |
| Hugging Face 認証 | **不要** |
| 想定環境 | Google Colab，T4（VRAM 約 14GB）推奨．CPU でも可 |
| 出力 | 最大 3 顔，1 顔あたり最大 478 点（虹彩含む） |

---

## デモでできること

1. **カメラ**で顔を撮影してランドマークを検出する  
2. 画像ファイルを**アップロード**して検出する  
3. 用意した**サンプル顔写真**（日本人・アジア系を含む）をクリックしてすぐ試せる  
4. 顔のメッシュ・輪郭・虹彩の重ね描きと，検出点数のサマリーを見る  

---

## Colab での実行手順

### 1. ランタイムを GPU にする（任意）

1. ノートブックを Colab で開く  
2. メニュー **ランタイム → ランタイムのタイプを変更**  
3. ハードウェア アクセラレータを **T4 GPU** にする（CPU でも動作します）  

### 2. セルを上から実行する

途中で止まっても再開しやすいよう，処理は次の段に分かれています．

| 順番 | セル | 内容 |
| :---: | :--- | :--- |
| 0 | 設定 | Webカメラの左右反転・描画オプション・顔の最大数 |
| 1 | ライブラリのインストール | `mediapipe` の導入 |
| 2 | ライブラリの読み込み・初期化 | モデル／サンプル画像 DL，検出器の構築 |
| 3 | Gradio の実行 | Web UI を起動する |

**Webカメラが左右反転しているとき**  
ノートブック上部の設定セルで `MIRROR_WEBCAM = True` / `False` を切り替え，Gradio 起動セルを再実行してください．

**メッシュが見づらいとき**  
設定セルの `DRAW_TESSELATION` / `DRAW_CONTOURS` / `DRAW_IRISES` / `DRAW_LANDMARK_DOTS` を切り替えてください．

**注意（インストール直後）**  
インストールのあとでインポートエラーが出る場合は，**ランタイム → セッションを再起動**し，設定セルとセル2からやり直してください．

### 3. UI の使い方

Gradio が起動すると，Colab 内に UI が表示されます（`share=True` のため公開用 URL も出ます）．

1. **サンプル画像**をクリックする，またはカメラで撮影／ファイルをアップロードする  
2. **ランドマークを検出**ボタンを押す（画像を選んだだけで自動実行される場合もあります）  
3. 右側に  
   - **検出結果**（メッシュ・輪郭の可視化）  
   - **検出サマリー**（顔の数・点数）  
   が表示されます  

---

## うまく検出させるコツ

- 顔が**正面**を向き，明るく，画面内で大きめに写るようにする  
- マスク・強い逆光・極端な横顔は検出が難しくなります  
- 複数人を写す場合は，顔同士が重ならないようにする（最大 3 顔）  

---

## 技術のポイント（説明用）

- **顔ランドマーク**: 顔の上の決まった位置（目尻・鼻先・口角など）を点として推定する技術  
- **メッシュ**: 点どうしを線でつないだ三角分割で，顔の立体的な形を近似する  
- モデル: MediaPipe Face Landmarker（Google 公式の `.task` モデル）  
- 推論は Colab 内で完結し，外部の顔解析 API は使いません  

応用例の話のネタ: AR フィルタ，バーチャルアバター，視線・表情の解析の前処理など．

---

## サンプル画像について

初期化セルで次の公開画像をダウンロードします（日本人・アジア系を含む）．

| ファイル | 内容 |
| :--- | :--- |
| `happy_japanese_woman.jpg` | Wikimedia・日本人（笑顔） |
| `happy_japanese_smile.jpg` | Wikimedia・日本人（スマイル） |
| `neutral_japanese.jpg` | Wikimedia・日本人 |
| `asian_portrait.jpg` ほか | Pexels の公開ポートレート |

カメラが使えない環境でも，サンプルだけでデモできます．

---

## Hugging Face について

このデモは Hugging Face のモデルを使いません．  
MediaPipe の公式モデル URL から `.task` ファイルを取得します．

**ユーザー認証は不要**です．

---

## トラブルシュート

| 症状 | 対処 |
| :--- | :--- |
| 顔を検出できない | 明るさ・距離・向きを見直す．正面で大きく写す |
| カメラが使えない | ブラウザのカメラ許可を確認．難しければアップロード／サンプルを使う |
| メッシュが Dense すぎる | `DRAW_TESSELATION = False` にして輪郭だけにする |
| `mediapipe` の import エラー | セッション再起動 → セル2から再実行 |
| モデル DL が遅い | 初回のみ．しばらく待つ，またはランタイム再起動後に再実行 |

---

## ライセンスメモ

利用する画像・モデルの条件は，各 Wikimedia / Pexels / MediaPipe（Google）の表記に従ってください．  
オープンキャンパス等の教育・デモ用途を想定しています．
