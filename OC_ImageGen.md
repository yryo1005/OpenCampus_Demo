# 画像生成デモ（Stable Diffusion）使い方

高校生向けオープンキャンパス用の，**テキストから画像を生成する**デモです．  
Google Colab（GPU: T4 推奨）上で Stable Diffusion v1.5 を動かし，Gradio の画面から日本語／英語のプロンプトを渡して画像を作ります．

| 項目 | 内容 |
| :--- | :--- |
| ノートブック | `OC_ImageGen.ipynb` |
| 主な技術 | Stable Diffusion v1.5（Diffusers），Gemini（日本語→英語翻訳），Gradio |
| API キー | **Gemini が必要**（`tokens.json` の `gemini` キー） |
| Hugging Face 認証 | **現時点では不要**（モデルは公開） |
| 想定環境 | Google Colab，T4（VRAM 約 14GB） |

---

## デモでできること

1. **日本語**または**英語**の文章（プロンプト）を入力する  
2. 日本語の場合は Gemini が **英語に翻訳**する（翻訳結果も画面に表示）  
3. Stable Diffusion が **512×512 の画像**を生成する  
4. 用意した**サンプルプロンプト**をクリックしてすぐ試せる  

---

## 事前準備：`tokens.json`

ノートブックと同じ場所に，次の形式の `tokens.json` を置いてください（リポジトリには含まれません）．

```json
{
  "gemini": "ここに Gemini API キー"
}
```

Colab で GitHub バッジから開いた場合は，左のファイル欄へ `tokens.json` をアップロードしてください．

---

## Hugging Face について

このデモで使うモデルは次の公開リポジトリです．

- https://huggingface.co/sd-legacy/stable-diffusion-v1-5

**現時点ではユーザー認証やライセンス同意は不要**です．  
将来モデルが gated になった場合は，上記ページでアクセス申請し，必要なら `tokens.json` に `huggingface_hub` トークンを追加してログインしてください．

---

## Colab での実行手順

### 1. ランタイムを GPU にする

1. ノートブックを Colab で開く  
2. メニュー **ランタイム → ランタイムのタイプを変更**  
3. ハードウェア アクセラレータを **T4 GPU** にする  

### 2. セルを上から実行する

途中で止まっても再開しやすいよう，処理は次の段に分かれています．

| 順番 | セル | 内容 |
| :---: | :--- | :--- |
| 0 | 設定 | ステップ数・画像サイズ・Gemini モデル名など |
| 1 | ライブラリのインストール | `diffusers` / `google-genai` を更新 |
| 2 | ライブラリの読み込み・初期化 | `tokens.json` 読み込み，モデルダウンロード |
| 3 | Gradio の実行 | Web UI を起動する |

**注意（インストール直後）**  
インストールのあとでインポートエラーが出る場合は，**ランタイム → セッションを再起動**し，設定セルとセル2からやり直してください．

### 3. UI の使い方

Gradio が起動すると，Colab 内に UI が表示されます（`share=True` のため公開用 URL も出ます）．

1. **サンプルプロンプト**をクリックする，または自分で文章を書く  
2. （任意）シードを指定する（同じ絵を再現したいとき．`-1` でランダム）  
3. **画像を生成**ボタンを押す  
4. 右側に生成画像と，実際に使った英語プロンプトが表示される  

生成にはおおよそ数十秒かかることがあります（ステップ数や GPU の空き具合による）．

---

## うまく書くコツ

- 「何が・どこで・どんな画風か」を短く書く（例: `桜並木を歩くロボット，アニメ風`）  
- 英語で直接書いてもよい（翻訳はスキップされる）  
- 文字をきれいに描かせるのは苦手なモデルなので，「看板の文字」などは期待しすぎない  

---

## 技術のポイント（説明用）

- **テキスト画像生成**: 文章の意味を手がかりに，ノイズから徐々に絵を形作る  
- **Stable Diffusion**: 潜在空間（小さい特徴マップ）で拡散過程を行うため，比較的少ない計算資源で動く  
- **プロンプト翻訳**: モデルは英語キャプション中心に学習されているため，日本語は英語にしてから渡す  

---

## トラブルシュート

| 症状 | 対処 |
| :--- | :--- |
| `tokens.json` が見つからない | ファイルをアップロードし，カレントディレクトリを確認 |
| `CUDA out of memory` | ランタイム再起動．`IMAGE_SIZE` を 512 のままにする |
| 生成が極端に遅い | ランタイムが GPU か確認．`NUM_INFERENCE_STEPS` を 20 前後に |
| 真っ黒／不適切判定 | プロンプトを変える．シードを変えて再生成 |
| インストール後に import できない | セッション再起動 → セル2から再実行 |

---

## ライセンスメモ

Stable Diffusion v1.5 は **CreativeML OpenRAIL-M** ライセンスです．  
オープンキャンパス等の教育・デモ用途を想定しています．有害・差別的な生成は避けてください．
