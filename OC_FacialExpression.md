# 表情認識デモ（Facial Expression Recognition）使い方

高校生向けオープンキャンパス用の，**顔写真から表情を推定する**デモです．  
Google Colab（GPU: T4 推奨）上で Vision Transformer を動かし，Gradio の画面からカメラ撮影や画像を渡して表情を認識します．

| 項目 | 内容 |
| :--- | :--- |
| ノートブック | `OC_FacialExpression.ipynb` |
| 主な技術 | ViT 表情認識（`trpakov/vit-face-expression`），OpenCV 顔検出，Gradio |
| API キー | **不要**（すべて Colab 内で推論） |
| Hugging Face 認証 | **不要**（モデルは公開） |
| 想定環境 | Google Colab，T4（VRAM 約 14GB） |
| 認識クラス | 喜び・悲しみ・怒り・驚き・恐れ・嫌悪・無表情（7 種類） |

---

## デモでできること

1. **カメラ**で顔を撮影して表情を推定する  
2. 画像ファイルを**アップロード**して推定する  
3. 用意した**サンプル顔写真**（日本人・アジア系を含む）をクリックしてすぐ試せる  
4. 検出した顔の枠と，各表情の**確信度ランキング**を見る  

---

## Colab での実行手順

### 1. ランタイムを GPU にする

1. ノートブックを Colab で開く  
2. メニュー **ランタイム → ランタイムのタイプを変更**  
3. ハードウェア アクセラレータを **T4 GPU** にする  

### 2. セルを上から実行する

途中で止まっても再開しやすいよう，処理は次の段に分かれています．

| 順番 | セル | 内容 |
| :---: | :--- | :--- |
| 0 | 設定 | Webカメラの左右反転・モデル ID |
| 1 | ライブラリのインストール | `transformers` を更新 |
| 2 | ライブラリの読み込み・初期化 | サンプル画像 DL，顔検出・表情モデル読み込み |
| 3 | Gradio の実行 | Web UI を起動する |

**Webカメラが左右反転しているとき**  
ノートブック上部の設定セルで `MIRROR_WEBCAM = True` / `False` を切り替え，Gradio 起動セルを再実行してください．

**注意（インストール直後）**  
インストールのあとでインポートエラーが出る場合は，**ランタイム → セッションを再起動**し，設定セルとセル2からやり直してください．

### 3. UI の使い方

Gradio が起動すると，Colab 内に UI が表示されます（`share=True` のため公開用 URL も出ます）．

1. **サンプル画像**をクリックする，またはカメラで撮影／ファイルをアップロードする  
2. **表情を認識**ボタンを押す（画像を選んだだけで自動実行される場合もあります）  
3. 右側に  
   - **検出結果**（顔の枠と推定ラベル）  
   - **推定結果**（7 表情の確信度ランキング）  
   が表示されます  

---

## うまく認識させるコツ

- 顔が**正面**を向き，明るく，画面内で大きめに写るようにする  
- マスク・強い逆光・横顔は検出や認識が難しくなります  
- 表情ははっきりしているほど判定しやすいです（わずかな笑顔などは「無表情」になりやすいことがあります）  
- 日本人の顔写真でも動作しますが，学習データ（FER2013）の偏りで誤ることがあります  

---

## 技術のポイント（説明用）

- **顔検出**: 画像のどこに顔があるかを見つける（このデモでは OpenCV の Haar Cascade）  
- **表情認識**: 切り出した顔画像を 7 クラスに分類する（Vision Transformer）  
- モデル: [trpakov/vit-face-expression](https://huggingface.co/trpakov/vit-face-expression)（FER2013 で学習）  
- 推論は Colab の GPU 上で完結し，外部の表情認識 API は使いません  

---

## サンプル画像について

初期化セルで次の公開画像をダウンロードします（日本人・アジア系を含む）．

| ファイル | 内容 |
| :--- | :--- |
| `happy_japanese_woman.jpg` | 喜び（Wikimedia・日本人） |
| `happy_japanese_smile.jpg` | 喜び（Wikimedia・日本人） |
| `neutral_japanese.jpg` | 無表情寄り（Wikimedia・日本人） |
| `asian_portrait.jpg` ほか | Pexels の公開ポートレート（笑い・真剣・驚き寄り） |

カメラが使えない環境でも，サンプルだけでデモできます．

---

## Hugging Face について

このデモで使うモデルは次の公開リポジトリです．

- https://huggingface.co/trpakov/vit-face-expression

**現時点ではユーザー認証やライセンス同意は不要**です．

---

## トラブルシュート

| 症状 | 対処 |
| :--- | :--- |
| 顔を検出できない | 明るさ・距離・向きを見直す．正面で大きく写す |
| カメラが使えない | ブラウザのカメラ許可を確認．難しければアップロード／サンプルを使う |
| 推定がおかしい | 別の表情をもう一度試す．サンプル画像で動作確認する |
| `CUDA out of memory` | ランタイム再起動後，他の重いノートを閉じる |
| インストール後に import できない | セッション再起動 → セル2から再実行 |

---

## ライセンスメモ

利用する画像・モデルの条件は，各 Wikimedia / Pexels / Hugging Face の表記に従ってください．  
オープンキャンパス等の教育・デモ用途を想定しています．
