# セグメンテーションデモ（Segment Anything）使い方

高校生向けオープンキャンパス用の，**写真の中の物体を色分け・切り出す**デモです．  
Google Colab（GPU: T4 推奨）上で Meta の Segment Anything Model（SAM）を動かし，Gradio の画面からカメラ撮影や画像を渡してセグメンテーションします．

| 項目 | 内容 |
| :--- | :--- |
| ノートブック | `OC_SegmentAnything.ipynb` |
| 主な技術 | Segment Anything（`vit_b`），Gradio，OpenCV |
| API キー | **不要**（すべて Colab 内で推論） |
| Hugging Face 認証 | **不要**（チェックポイントは Meta 公式 CDN） |
| 想定環境 | Google Colab，T4（VRAM 約 14GB） |
| モード | 自動（すべて色分け）／点指定（クリックした物体） |

---

## デモでできること

1. **カメラ**で撮影した写真を，物体ごとに色分けする  
2. 画像ファイルを**アップロード**して分割する  
3. 用意した**サンプル画像**（犬・猫・人物・果物・自転車など）をクリックしてすぐ試せる  
4. **点指定**で，クリックした場所の物体だけを切り出す  

---

## Colab での実行手順

### 1. ランタイムを GPU にする

1. ノートブックを Colab で開く  
2. メニュー **ランタイム → ランタイムのタイプを変更**  
3. ハードウェア アクセラレータを **T4 GPU** にする  

### 2. セルを上から実行する

途中で止まっても再開しやすいよう，処理は次の段に分かれています．

| 順番 | セル | 内容 |
| :---: | :--- | :--- |
| 0 | 設定 | Webカメラの左右反転・モデル種別・分割密度 |
| 1 | ライブラリのインストール | `segment-anything` の導入 |
| 2 | ライブラリの読み込み・初期化 | サンプル画像・チェックポイント DL，モデル読み込み |
| 3 | Gradio の実行 | Web UI を起動する |

**Webカメラが左右反転しているとき**  
ノートブック上部の設定セル，または UI の「左右反転（ミラー）」で切り替えてください．

**注意（インストール直後）**  
インストールのあとでインポートエラーが出る場合は，**ランタイム → セッションを再起動**し，設定セルとセル2からやり直してください．

**初回の待ち時間**  
SAM の重み（`vit_b` で約 375MB）を Meta のサーバから取得するため，初回は数分かかることがあります．

### 3. UI の使い方

Gradio が起動すると，Colab 内に UI が表示されます（`share=True` のため公開用 URL も出ます）．

#### 自動（すべて色分け）

1. **サンプル画像**をクリックする，またはカメラで撮影／ファイルをアップロードする  
2. モードを **自動（すべて色分け）** にする  
3. **セグメンテーション実行**を押す  
4. 右側に色分け結果と，いちばん大きい領域の切り抜きが表示される  

#### 点指定（クリック）

1. 画像を用意する  
2. モードを **点指定（クリック）** にする  
3. **左側の入力画像**の「切り出したい場所」をクリックする  
4. クリックするたびに点が追加され，その物体のマスクが更新される  
5. 点がおかしくなったら **クリック点をリセット**  

---

## うまく分割させるコツ

- 物体の輪郭がはっきりし，背景と色が違う写真が向いています  
- 自動モードは少し時間がかかります（デモ用に分割密度を抑えています）  
- 点指定は，物体の**内側**をクリックすると安定しやすいです  
- 細かすぎる模様や強いブレは，マスクが割れやすいです  

---

## 技術のポイント（説明用）

- **セグメンテーション**: 画像のどのピクセルがどの物体に属するかを決める処理  
- **SAM（Segment Anything）**: 「どこでも切れる」ように学習された基盤モデル  
- **自動分割**: 画像全体に点を打ち，たくさんのマスク候補を生成する  
- **点プロンプト**: 「この点が含まれる物体」を指示して 1 つのマスクを得る  
- モデル: Meta 公式 [segment-anything](https://github.com/facebookresearch/segment-anything) の `vit_b`  
- 推論は Colab の GPU 上で完結し，外部のセグメンテーション API は使いません  

---

## サンプル画像について

初期化セルで次の公開画像をダウンロードします（撮影しなくてもデモできます）．

| ファイル | 内容 |
| :--- | :--- |
| `dog.jpg` | 犬（Pexels） |
| `cat.jpg` | 猫（Pexels） |
| `person_asian.jpg` | 人物・アジア系（Pexels） |
| `japanese_smile.jpg` | 人物・日本人（Wikimedia） |
| `fruits.jpg` | 果物（Pexels） |
| `bicycle.jpg` | 自転車（Pexels） |

---

## Hugging Face について

このデモの重みは Hugging Face ではなく，Meta 公式の配布 URL から取得します．

- https://dl.fbaipublicfiles.com/segment_anything/sam_vit_b_01ec64.pth  

**ユーザー認証やライセンス同意の操作は不要**です．

---

## トラブルシュート

| 症状 | 対処 |
| :--- | :--- |
| 自動分割が遅い | 設定の `POINTS_PER_SIDE` を小さくする（例: 12）．`MAX_IMAGE_SIDE` を下げる |
| マスクがざっくりしすぎる | `POINTS_PER_SIDE` を上げる．点指定モードを試す |
| クリック位置がずれる | 左右反転のオン／オフを確認し，点をリセットしてから再クリック |
| カメラが使えない | ブラウザのカメラ許可を確認．難しければアップロード／サンプルを使う |
| `CUDA out of memory` | ランタイム再起動．`vit_b` のままにする．`MAX_IMAGE_SIDE` を 768 などに下げる |
| インストール後に import できない | セッション再起動 → セル2から再実行 |

---

## ライセンスメモ

利用する画像・モデルの条件は，各 Pexels / Wikimedia / Meta Segment Anything の表記に従ってください．  
オープンキャンパス等の教育・デモ用途を想定しています．
